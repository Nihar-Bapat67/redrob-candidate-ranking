# ---- Large input data (provided by organizers, not version-controlled) ----
candidates.jsonl
*.db
*.db-wal
*.db-shm

# ---- Virtual environments ----
venv/
.venv/

# ---- Python caches ----
__pycache__/
*.pyc
*.pyo

# ---- Derived / regenerable artifacts ----
coherence_scores.csv
coherence_ceiling.json
feature_matrix.csv
artifacts/*
# keep the small trained model + feature order (needed by the sandbox app); big
# index/embeddings/model-weights stay ignored. (gitignore has no inline comments!)
!artifacts/ranker.lgb
!artifacts/feature_cols.json
label_queue.jsonl
_batch_labels.json
_precompute_*.log

# ---- Large precompute artifacts (regenerate via precompute.py) ----
# (artifacts/ contents handled above: only ranker.lgb + feature_cols.json kept)
artifacts_test/
*.npy
*.index
*.faiss

# ---- Secrets (NEVER commit) ----
.env
*.key

# ---- Scratch / temp ----
_stage3_*.txt
_broadnet*.log
_streamlit.log
_spec_*
*.tmp

# ---- OS cruft ----
.DS_Store
Thumbs.db

# local test scaffolding
organic_candidates.jsonl
test.py

# doc build helper
_md2docx.py

# Release bundle (uploaded to GitHub Releases, not committed)
*.tar.gz
